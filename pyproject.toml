[project]
name = "schlieren"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Fritz Mueller", email = "fritzm@fritzm.org" }
]
requires-python = ">=3.12"
dependencies = [
    "cadquery>=2.8.0",
    "ocp-vscode>=4.1.0",
]

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 110
